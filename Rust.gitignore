# 🌐 本文档由 github/gitignore (https://github.com/github/gitignore) 翻译,英文原版见原项目。

# 由 Cargo 生成
# 包含编译产物和可执行文件
debug
target

# rustfmt 生成的备份文件
**/*.rs.bk

# rustc 的 MSVC Windows 构建会生成这些文件,用于存储调试信息
*.pdb

# 由 cargo mutants 生成
# 包含变异测试数据
**/mutants.out*/

# rustc 遇到内部编译器错误时会在当前目录转储堆栈信息
rustc-ice-*.txt

# RustRover
#  JetBrains 专属模板单独维护在 JetBrains.gitignore 中,地址:
#  https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#  可加入全局 gitignore 或合并进本文件。若想用更彻底(不推荐)的方案,
#  可取消下一行注释,忽略整个 idea 目录。
#.idea/
