# 🌐 本文档由 github/gitignore (https://github.com/github/gitignore) 翻译,英文原版见原项目。

.gradle
**/build/
!**/src/**/build/

# 忽略 Gradle GUI 配置
gradle-app.setting

# 避免忽略 Gradle wrapper 的 jar 文件(.jar 通常会被忽略)
!gradle-wrapper.jar

# 避免忽略 Gradle wrapper 属性文件
!gradle-wrapper.properties

# 项目缓存
.gradletasknamecache

# Eclipse Gradle 插件生成的文件
# Eclipse Core
.project
# JDT 专属(Eclipse Java Development Tools)
.classpath
